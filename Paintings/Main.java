#include<stdio.h>
int main()
{
  int a,b,c,d,e,f;
  scanf("%d%d%d%d%d%d",&a,&b,&c,&d,&e,&f);
  if((2*(a+b))>=((2*(c+d))+2*(e+f)))
    printf("Raj can fix both painting");
    else
      printf("Raj cannot fix both painting");
  
  //fill your code
}